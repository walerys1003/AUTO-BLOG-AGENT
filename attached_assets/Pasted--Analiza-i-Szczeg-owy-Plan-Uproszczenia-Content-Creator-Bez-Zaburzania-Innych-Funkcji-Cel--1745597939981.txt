🔍 Analiza i Szczegółowy Plan Uproszczenia Content Creator – Bez Zaburzania Innych Funkcji:

🎯 Cel operacyjny:
Uproszczenie istniejącego systemu Content Creator, zachowując integralność i kompatybilność z resztą systemu.

Zminimalizowanie logiki wewnętrznej Content Creator bez wpływu na inne moduły, takie jak:

Blogi, Baza tematów (ArticleTopic), Automatyzacje (AutomationRule).

Skupienie się na uproszczeniu przepływu: Temat → Generacja → Edycja → Publikacja.

🧠 Kluczowe Obszary Do Uproszczenia (w kontekście kodu):
1️⃣ Widoki / Routing:
Obecnie:

Kilka widoków: /content-generator, /standard-editor, /content-creator/edit/..., /preview/..., automatyzacje.

Złożone warunki wyboru bloga, statusu, dynamiczne generowanie.

Do zmiany:

Zlikwidować:

Widok /content-generator → niepotrzebny tryb.

Widok /standard-editor → zastąpić jednym edytorem.

Całą obsługę automatyki w Content Creator (pozostawić ją osobno, jeśli konieczna).

Zachować:

Jeden uproszczony widok: /content-creator

Przenieść całą funkcję edycji i generacji do jednej strony.

2️⃣ Model danych – ContentLog & ArticleTopic:
Obecnie:

ContentLog z error_message jako magazyn JSON.

Powiązania z ArticleTopic (status, keywords, score, blog_id).

Drafty, statusy, wersjonowanie.

Do zmiany:

ContentLog:

Zastąpić nowym modelem Content:

python
Kopiuj
Edytuj
class Content(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    title = db.Column(db.String(255))
    topic = db.Column(db.String(255))
    body = db.Column(db.Text)
    status = db.Column(db.String(50))  # draft / published
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
Przenieść dane z error_message do jasnych kolumn: title, body.

ArticleTopic:

Uprościć: pobierać tylko tytuł i score – bez powiązanej logiki.

Zachować status = 'approved' jako filtr.

3️⃣ Funkcja Generowania:
Obecnie:

Dynamiczna generacja paragrafów z obsługą statusów, stylów, liczby akapitów.

Podział na dynamiczne i standardowe tryby.

Do zmiany:

Usunąć dynamiczne generowanie akapitów.

Jedno proste zapytanie do Claude Sonnet API:

python
Kopiuj
Edytuj
POST /generate-content
Input: { "topic": "...", "style": "...", "paragraphs": 5 }
Output: { "content": "..." }
Stały prompt:

"Wygeneruj artykuł o temacie: {topic}, styl: {style}, długość: {paragraphs} akapitów."

Zlikwidować: obsługę regeneracji, indeksowania akapitów, dynamicznych flag.

